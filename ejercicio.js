console.log ("Buenas noches");
console.log("Que tu vida sea larga y util como el papel higienico");

let numeros = [1,2,3,4];
function verArray(numeros){
    for(let i=0; i<numeros.length; i++){
    console.log(numeros[i]);
   };
}
